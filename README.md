# Malik Store
A mobile-friendly static Free Fire diamond store prototype.

Open `index.html` in a browser.
- Prices can be changed from `admin.html` and are saved in browser localStorage.
- Cart and demo order creation work locally.
- Real authentication, database, server-side order handling and a legitimate payment gateway still need a backend and deployment.
